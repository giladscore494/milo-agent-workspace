import { describe, expect, it } from 'vitest';
import { initialWorkspaceState, reconstructRun, reduceRunEvent } from '../lib/runReducer';
it('deduplicates and reconstructs from last event stream', () => { const event:any={id:'1',run_id:'r',event_type:'source_recorded',agent:'Analyst',payload:{id:'s',title:'A',domain:'example.com',source_type:'primary',source_strength:'strong',agent:'Analyst'}}; const s=reduceRunEvent(initialWorkspaceState,event); expect(reduceRunEvent(s,event).events).toHaveLength(1); const r=reconstructRun({id:'r',conversation_id:'c',status:'running'} as any,[event]); expect(r.sources).toHaveLength(1); expect(r.agents.Analyst.sources).toHaveLength(1); });
it('tracks internet approval badges and quotas',()=>{ const s=reduceRunEvent(initialWorkspaceState,{id:'2',run_id:'r',event_type:'tool_access_granted',agent:'Scout',payload:{reason:'freshness required',domains:['nhtsa.gov']}} as any); expect(s.agents.Scout.internet).toBe('approved'); expect(s.agents.Scout.domains).toEqual(['nhtsa.gov']); });
