"""The canonical WorkScope: what a mapping plan intends, built from chat or clicks.

`contract.py` is the one record and its digest, `directory.py` the reviewed
marques a plan may name, `interpret.py` the deterministic reader of a typed
instruction, `coverage.py` the bounded read of what the canonical catalog
already holds, and `service.py` the membership-authorized operations the API
exposes. Nothing here prepares Government data, creates a run or calls a
provider: a WorkScope is a PLAN, and in this release it is only ever a draft.
"""
